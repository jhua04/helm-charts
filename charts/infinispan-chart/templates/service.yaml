apiVersion: v1
kind: Service
metadata:
  name: {{ include "infinispan-chart.fullname" . }}
  labels:
    {{- include "infinispan-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "infinispan-chart.selectorLabels" . | nindent 4 }}
